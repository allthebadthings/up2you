{
  "name": "up2you-monorepo",
  "version": "1.0.0",
  "description": "Up2You Jewelry Shop and Admin Platform",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../jewelry && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix jewelry\"",
    "build": "cd backend && npm run build && cd ../jewelry && npm run build",
    "package": "node scripts/package.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
